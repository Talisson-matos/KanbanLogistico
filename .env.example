# URL base da API backend (server/). Aponte para onde o servidor
# Express estiver rodando. Em desenvolvimento local, o padrão já
# funciona sem precisar criar um .env (ver src/config/api.ts).
VITE_API_BASE_URL=http://localhost:4000/api
